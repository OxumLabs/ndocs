version: 2

# Build settings
build:
  os: ubuntu-20.04
  tools:
    python: "3.0"

# Python installation settings
python:
  install:
    - method: pip
      path: .

# MkDocs settings
mkdocs:
  configuration: mkdocs.yml
